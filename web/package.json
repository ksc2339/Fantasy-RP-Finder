{
  "name": "web",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "data:roster-resource": "node scripts/generate-roster-resource-roles.mjs",
    "data:roster-enrich-teams": "node scripts/enrich-roster-resource-teamslug.mjs",
    "data:oopsy": "node scripts/generate-oopsy-projections.mjs"
  },
  "dependencies": {
    "preact": "^10.28.4"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.10.3",
    "@types/node": "^24.12.0",
    "typescript": "~5.9.3",
    "vite": "^8.0.0"
  }
}
